// This will be copied to the dist directory; configuring TypeScript to generate
// this file wasn't worth the effort.

export default function qcircuit(
  renderTex: (tex: string) => string,
  tex: string
): string;
